apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pipe-services-ingress
  annotations:
    nginx.ingress.kubernetes.io/service-ipstream: "true"
spec:
  rules:
  - host: 3ed3-2001-818-e975-8500-564-a193-df48-674b.ngrok-free.app
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hello-world-service
            port:
              number: 8080
      - path: /dotnet
        pathType: Prefix
        backend:
          service:
            name: dotnet-pipe-service
            port:
              number: 8080
      - path: /java
        pathType: Prefix
        backend:
          service:
            name: the-one-pipe-service
            port:
              number: 8080